import React from 'react'
import { useNavbar as LanerNavbar } from '../Navbar'

export const MainLayout = () => {
    return(
        <div>
            <LanerNavbar />
            MAINLAYOUT
        </div>
    )
}