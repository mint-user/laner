import {React} from 'react'
import {useNavbar as LanerNavbar} from '../Navbar'
import {LoginForm} from './LoginForm'

export const Login = () => {
  return (
    <div>
      {/* <LanerNavbar /> */}
      <LoginForm />
    </div>
  );
}
